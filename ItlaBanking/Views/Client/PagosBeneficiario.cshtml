@model ItlaBanking.ViewModels.BeneficiarioViewModel
@{
}
<html>
<body class="bg-light" id="CardViewFormulario">
    <div class="container bg-warning border border-white rounded shadow-lg p-1 mb-5 bg-white rounded">

        <div>
            <div class="card-header headNombre rounded border border-secondary shadow-sm p-1 bg-white rounded mb-3">
                <div class="d-flex justify-content-center letterPagos">Pagos Beneficiarios</div>
            </div>

            <form asp-action="PagosBeneficiario" asp-controller="Client" method="post">

                <div class="form-row">
                    <div class="form-group col-md-6">
                        <label class="labelPago ml-3">Seleccionar Beneficiario:</label>
                        <select asp-for="NumeroCuentaPagar" class="custom-select custom-select-lg mb-3 rounded-pill shadow bg-white rounded border border-secondary">
                            <option value="0" selected>Beneficiarios..</option>
                            @foreach (var beneficiario in Model.cuenta)
                            {
                                <option value="@beneficiario.NumeroCuenta">@beneficiario.NumeroCuenta</option>
                            }
                        </select>
                    </div>
                    <div class="form-group col-md-6">
                        <label class="labelPago ml-3">¿Desde que cuenta quieres pagar?</label>
                        <select asp-for="NumeroCuenta" class="custom-select custom-select-lg mb-3 rounded-pill shadow bg-white rounded border border-secondary">
                            <option value="0" selected>Seleccionar Cuenta</option>
                            @foreach (var cuentauser in Model.cuentauser)
                            {
                                <option value="@cuentauser.NumeroCuenta">@cuentauser.NumeroCuenta</option>
                            }
                        </select>
                    </div>

                </div>

                <div class="form-row">
<<<<<<< HEAD

=======
                    
>>>>>>> 210e86134700a8d31e8b4e3eefbf3645b79adb5e

                    <div class="form-group col-md-6">
                        <label class="labelPago ml-3">Monto a pagar: </label>
                        <input asp-for="Monto" type="number" min="0.01" step="0.01" class="form-control form-control-lg rounded-pill border border-success shadow-sm p-3  bg-white rounded" placeholder="Ingresar Monto">
                    </div>

                </div>

                <div class="d-flex justify-content-center mt-3 mb-2">
                    <input type="submit" value="Pagar" class="btn btn-outline-success font-weight-bold  shadow-lg rounded w-25">

                </div>

            </form>



        </div>

    </div>

</body>
</html>

